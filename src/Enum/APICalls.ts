export enum APICalls {    //is a special data type in TypeScript and many other programming languages that represents a collection of named values
    GET = "GET",
    POST = "POST",
    PUT = "PUT",
    DELETE = "DELETE",
    OPTIONS= "OPTIONS",
    HEAD = "HEAD"
}